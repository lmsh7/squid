//! Shared simulation modules, used by both the game binary (`main.rs`) and the
//! headless capture binary (`bin/capture.rs`).

pub mod camera;
pub mod components;
pub mod config;
pub mod flocking;
pub mod setup;
pub mod ui;
pub mod water;
